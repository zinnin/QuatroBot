<Window x:Class="Quatro.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quatro.Wpf"
        mc:Ignorable="d"
        Title="Quatro" Height="700" Width="900" 
        Background="#2D2D30">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Status Bar -->
        <TextBlock x:Name="StatusText" 
                   Grid.ColumnSpan="2"
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"
                   Text="Player 1: Select a piece to give" />

        <!-- Game Board -->
        <Border Grid.Row="1" Grid.Column="0" 
                Background="#3E3E42" 
                CornerRadius="10"
                Margin="10"
                Padding="20">
            <UniformGrid x:Name="BoardGrid" 
                         Rows="4" Columns="4" />
        </Border>

        <!-- Available Pieces Panel -->
        <Border Grid.Row="1" Grid.Column="1" 
                Background="#3E3E42" 
                CornerRadius="10"
                Margin="10"
                Padding="10">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" 
                           Text="Available Pieces" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10" />
                
                <UniformGrid x:Name="PiecesGrid" 
                             Rows="4" Columns="4" />
            </DockPanel>
        </Border>

        <!-- Selected Piece Display -->
        <Border Grid.Row="2" Grid.ColumnSpan="2"
                Background="#3E3E42"
                CornerRadius="10"
                Margin="10"
                Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Piece to play: " 
                           FontSize="18" 
                           Foreground="White"
                           VerticalAlignment="Center" />
                <Border x:Name="SelectedPieceDisplay"
                        Width="60" Height="60"
                        Margin="10,0,20,0" />
                <Button x:Name="NewGameButton" 
                        Content="New Game" 
                        FontSize="16"
                        Padding="20,10"
                        Click="NewGameButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
